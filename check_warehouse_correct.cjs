const mysql = require('mysql2/promise'); async function run() { try { const db = await mysql.createConnection({ host: '127.0.0.1', port: 3307, user: 'nobalcasting_user', password: 'C0digix$309', database: 'nobalcasting' }); const [rows] = await db.execute('SELECT warehouse_id, warehouse_name FROM warehouse'); console.log(JSON.stringify(rows)); await db.end(); } catch (e) { console.error(e); } } run();  
